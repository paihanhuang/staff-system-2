SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_CHANNEL_IDS=C0ADMFN09P0

# Optional: restrict command execution to these Slack user IDs.
# Example: ALLOWED_USER_IDS=U12345,U67890
ALLOWED_USER_IDS=

# Polling interval (seconds)
POLL_INTERVAL_SECONDS=15

# Workspace root for codex execution
WORKSPACE_DIR=/home/etem/staff-system-3

# codex exec flags. Keep sandboxed for safety.
CODEX_EXEC_FLAGS=--full-auto

# Timeout per Slack command execution
CODEX_TIMEOUT_SECONDS=1800

# Start from latest message at daemon boot to avoid replaying backlog
START_FROM_LATEST=true

# Optional explicit env/state paths
# DAEMON_ENV_FILE=integrations/slack-codex-daemon/.env
# DAEMON_STATE_FILE=integrations/slack-codex-daemon/state.json
